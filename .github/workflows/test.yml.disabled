name: test
on: [pull_request]
jobs:
  integration:
    runs-on: ubuntu-22.04
    steps:
      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y sscg

      - name: Clone repository
        uses: actions/checkout@v2

      - name: Run test
        run: make check
